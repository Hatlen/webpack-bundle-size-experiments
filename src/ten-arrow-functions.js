const a = ({ name }) => {
  console.log(`Hello ${name}!`);
};
a({ name: "world" });

const b = ({ name }) => {
  console.log(`Hello ${name}!`);
};
b({ name: "world" });
const c = ({ name }) => {
  console.log(`Hello ${name}!`);
};
c({ name: "world" });

const d = ({ name }) => {
  console.log(`Hello ${name}!`);
};
d({ name: "world" });

const e = ({ name }) => {
  console.log(`Hello ${name}!`);
};
e({ name: "world" });

const f = ({ name }) => {
  console.log(`Hello ${name}!`);
};
f({ name: "world" });

const g = ({ name }) => {
  console.log(`Hello ${name}!`);
};
g({ name: "world" });

const h = ({ name }) => {
  console.log(`Hello ${name}!`);
};
h({ name: "world" });

const i = ({ name }) => {
  console.log(`Hello ${name}!`);
};
i({ name: "world" });

const j = ({ name }) => {
  console.log(`Hello ${name}!`);
};
j({ name: "world" });
